import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import { Header } from "@/app/components/Header";
import { Sidebar } from '@/app/components/Sidebar'
import { Arimo, Prompt, IBM_Plex_Sans } from 'next/font/google';
import MainLogo from '@/public/img/memorial-health-system.png';

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

const ibmPlexSans = IBM_Plex_Sans({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-ibm-plex-sans',
  weight: ['100', '200', '300', '400', '500', '600', '700'],
  style: ['normal', 'italic']
});

const arimo = Arimo({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-arimo',
  weight: ['400', '500', '600', '700'],
  style: ['normal', 'italic']
});

const prompt = Prompt({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-prompt',
  weight: ['100', '200', '300', '400', '500', '600', '700', '800', '900'],
  style: ['normal', 'italic']
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} ${ibmPlexSans.variable} ${arimo.variable} ${prompt.variable} antialiased`}>
      <body>
        <Header />
        <Sidebar 
          organizationName="Memorial Healthcare System"
          organizationLogo={MainLogo}
        />
        <main className="pl-64">
          {children}
        </main>
      </body>
    </html>
  );
}
